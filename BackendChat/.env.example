# Copiar como .env y ajustar según el entorno

# URL del frontend que consume el backend (para CORS)
FRONTEND_URL=http://localhost:5173

# Puerto donde escucha el backend
PORT=8000

# Redis (dentro de Docker el host es el nombre del servicio)
REDIS_HOST=redisdb
REDIS_PORT=6379
